# conversation-engine — full development install
# -----------------------------------------------------------------------
# macOS: if sounddevice fails with PortAudio error, run first:
#   brew install portaudio
# -----------------------------------------------------------------------

# Core (always required)
openai>=1.30.0
httpx>=0.27.0
numpy>=1.26.0
python-dotenv>=1.0.0

# ASR provider: DashScope Paraformer
dashscope>=1.20.0

# WebSocket server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0

# CLI audio I/O (local mic + speakers)
sounddevice>=0.4.7
